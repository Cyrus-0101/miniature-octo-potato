name: Deploy to Google App Engine
on:
  push:
    branches:
      - master
jobs:
  build:
    name: Mbuzi Munch API
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: google-github-actions/deploy-appengine@main
        with:
          credentials: ${{ secrets.gcp_credentials }}
